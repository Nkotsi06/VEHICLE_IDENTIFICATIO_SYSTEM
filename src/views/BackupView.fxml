<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.effect.DropShadow?>
<?import javafx.scene.layout.*?>

<BorderPane xmlns:fx="http://javafx.com/fxml/1" fx:controller="controllers.BackupController"
            stylesheets="@../css/application.css" styleClass="border-pane">

    <top>
        <fx:include source="MenuBarComponent.fxml"/>
    </top>

    <center>
        <ScrollPane fitToWidth="true" fitToHeight="false" hbarPolicy="NEVER" vbarPolicy="AS_NEEDED">
            <VBox spacing="20" alignment="TOP_CENTER" styleClass="dashboard-container">
                <padding>
                    <Insets top="25" right="40" bottom="30" left="40"/>
                </padding>

                <Label text="Backup Management" styleClass="label-title" alignment="CENTER" maxWidth="Infinity"/>
                <Label text="Create, schedule, and restore database backups" styleClass="label-subtitle" alignment="CENTER" maxWidth="Infinity"/>

                <Separator/>

                <ProgressIndicator fx:id="loadProgress" visible="false" maxWidth="40"/>

                <HBox alignment="CENTER">
                    <VBox maxWidth="800" styleClass="profile-edit-card" alignment="CENTER">
                        <Label text="Backup Configuration" styleClass="section-title" alignment="CENTER" maxWidth="Infinity"/>

                        <GridPane hgap="15" vgap="12" alignment="CENTER">
                            <columnConstraints>
                                <ColumnConstraints halignment="RIGHT" minWidth="100"/>
                                <ColumnConstraints minWidth="200"/>
                            </columnConstraints>

                            <Label text="Backup Type:" GridPane.rowIndex="0" GridPane.columnIndex="0" style="-fx-font-weight: bold;"/>
                            <ComboBox fx:id="backupTypeComboBox" GridPane.rowIndex="0" GridPane.columnIndex="1" prefWidth="200" promptText="Select backup type"/>

                            <Label text="Schedule:" GridPane.rowIndex="1" GridPane.columnIndex="0" style="-fx-font-weight: bold;"/>
                            <ComboBox fx:id="scheduleComboBox" GridPane.rowIndex="1" GridPane.columnIndex="1" prefWidth="200" promptText="Select schedule"/>

                            <Label text="Schedule Date:" GridPane.rowIndex="2" GridPane.columnIndex="0" style="-fx-font-weight: bold;"/>
                            <DatePicker fx:id="scheduleDatePicker" GridPane.rowIndex="2" GridPane.columnIndex="1" prefWidth="200"/>
                        </GridPane>

                        <HBox spacing="15" alignment="CENTER">
                            <Button fx:id="createBackupButton" text="Create Backup Now" styleClass="button-success" prefWidth="150"/>
                            <Button fx:id="scheduleBackupButton" text="Schedule Backup" styleClass="button-primary" prefWidth="130"/>
                            <Button fx:id="fadeButton" text="Animate" styleClass="button-warning" prefWidth="100"/>
                        </HBox>
                    </VBox>
                </HBox>

                <ProgressBar fx:id="operationProgress" prefWidth="Infinity" progress="0" visible="false"/>
                <Label fx:id="statusLabel" styleClass="status-text" alignment="CENTER" maxWidth="Infinity"/>

                <Separator/>

                <HBox alignment="CENTER">
                    <VBox maxWidth="800" styleClass="profile-edit-card" alignment="CENTER">
                        <Label text="Backup Statistics" styleClass="section-title" alignment="CENTER" maxWidth="Infinity"/>

                        <GridPane hgap="20" vgap="10" alignment="CENTER">
                            <columnConstraints>
                                <ColumnConstraints halignment="RIGHT" minWidth="100"/>
                                <ColumnConstraints minWidth="150"/>
                                <ColumnConstraints halignment="RIGHT" minWidth="100"/>
                                <ColumnConstraints minWidth="150"/>
                            </columnConstraints>

                            <Label text="Last Backup:" GridPane.rowIndex="0" GridPane.columnIndex="0" style="-fx-font-weight: bold;"/>
                            <Label fx:id="lastBackupLabel" GridPane.rowIndex="0" GridPane.columnIndex="1" styleClass="status-text"/>

                            <Label text="Backup Size:" GridPane.rowIndex="0" GridPane.columnIndex="2" style="-fx-font-weight: bold;"/>
                            <Label fx:id="backupSizeLabel" GridPane.rowIndex="0" GridPane.columnIndex="3" styleClass="status-text"/>

                            <Label text="Total Backups:" GridPane.rowIndex="1" GridPane.columnIndex="0" style="-fx-font-weight: bold;"/>
                            <Label fx:id="backupCountLabel" GridPane.rowIndex="1" GridPane.columnIndex="1" styleClass="status-text"/>
                        </GridPane>
                    </VBox>
                </HBox>

                <HBox alignment="CENTER">
                    <VBox maxWidth="800" styleClass="profile-edit-card" alignment="CENTER">
                        <Label text="Available Backups" styleClass="section-title" alignment="CENTER" maxWidth="Infinity"/>
                        <ListView fx:id="backupsList" prefHeight="200"/>
                        <HBox spacing="15" alignment="CENTER">
                            <Button fx:id="restoreButton" text="Restore Selected" styleClass="button-warning" prefWidth="130"/>
                            <Button fx:id="refreshButton" text="Refresh" styleClass="button-info" prefWidth="100"/>
                            <Button fx:id="deleteBackupButton" text="Delete Selected" styleClass="button-danger" prefWidth="130"/>
                        </HBox>
                    </VBox>
                </HBox>

                <HBox spacing="10" alignment="CENTER">
                    <Button fx:id="backButton" text="Back to Dashboard" styleClass="button-secondary" prefWidth="150"/>
                </HBox>

            </VBox>
        </ScrollPane>
    </center>

    <bottom>
        <fx:include source="FooterComponent.fxml"/>
    </bottom>
</BorderPane>