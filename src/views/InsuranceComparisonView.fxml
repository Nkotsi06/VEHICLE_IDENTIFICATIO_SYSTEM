<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<BorderPane xmlns:fx="http://javafx.com/fxml/1" fx:controller="controllers.InsuranceComparisonController"
            stylesheets="@../css/application.css" styleClass="border-pane">

    <top>
        <fx:include source="MenuBarComponent.fxml"/>
    </top>

    <center>
        <ScrollPane fitToWidth="true" fitToHeight="false" hbarPolicy="NEVER" vbarPolicy="AS_NEEDED">
            <VBox spacing="20" alignment="TOP_CENTER" styleClass="dashboard-container">
                <padding>
                    <Insets top="25" right="40" bottom="30" left="40"/>
                </padding>

                <Label text="Insurance Policy Comparison" styleClass="label-title" alignment="CENTER" maxWidth="Infinity"/>
                <Label text="Compare insurance policies side by side" styleClass="label-subtitle" alignment="CENTER" maxWidth="Infinity"/>

                <Separator/>

                <ProgressIndicator fx:id="loadProgress" visible="false" maxWidth="40"/>

                <!-- Select Policies to Compare -->
                <HBox alignment="CENTER">
                    <VBox maxWidth="900" minWidth="600" styleClass="profile-edit-card" alignment="CENTER">
                        <Label text="Select Policies to Compare" styleClass="section-title" alignment="CENTER" maxWidth="Infinity"/>

                        <GridPane hgap="15" vgap="12" alignment="CENTER">
                            <columnConstraints>
                                <ColumnConstraints halignment="RIGHT" minWidth="80" prefWidth="80"/>
                                <ColumnConstraints minWidth="300" hgrow="ALWAYS"/>
                            </columnConstraints>
                            <Label text="Policy 1:" GridPane.rowIndex="0" GridPane.columnIndex="0" style="-fx-font-weight: bold;"/>
                            <ComboBox fx:id="policy1ComboBox" GridPane.rowIndex="0" GridPane.columnIndex="1" prefWidth="350" promptText="Select first policy"/>

                            <Label text="Policy 2:" GridPane.rowIndex="1" GridPane.columnIndex="0" style="-fx-font-weight: bold;"/>
                            <ComboBox fx:id="policy2ComboBox" GridPane.rowIndex="1" GridPane.columnIndex="1" prefWidth="350" promptText="Select second policy"/>

                            <Label text="Policy 3:" GridPane.rowIndex="2" GridPane.columnIndex="0" style="-fx-font-weight: bold;"/>
                            <ComboBox fx:id="policy3ComboBox" GridPane.rowIndex="2" GridPane.columnIndex="1" prefWidth="350" promptText="Select third policy"/>
                        </GridPane>

                        <HBox alignment="CENTER" spacing="15">
                            <Button fx:id="compareButton" text="Compare Policies" styleClass="button-primary" prefWidth="150"/>
                            <Button fx:id="resetButton" text="Reset" styleClass="button-secondary" prefWidth="100"/>
                            <Button fx:id="fadeButton" text="Animate" styleClass="button-warning" prefWidth="100"/>
                        </HBox>
                    </VBox>
                </HBox>

                <ProgressBar fx:id="operationProgress" prefWidth="Infinity" progress="0" visible="false"/>

                <!-- Comparison Results -->
                <HBox alignment="CENTER">
                    <VBox maxWidth="1100" styleClass="profile-edit-card" alignment="CENTER">
                        <Label text="Comparison Results" styleClass="section-title" alignment="CENTER" maxWidth="Infinity"/>

                        <TableView fx:id="comparisonTable" prefHeight="400" maxWidth="Infinity">
                            <columnResizePolicy>
                                <TableView fx:constant="CONSTRAINED_RESIZE_POLICY"/>
                            </columnResizePolicy>
                            <columns>
                                <TableColumn fx:id="criteriaColumn" text="Criteria" prefWidth="180"/>
                                <TableColumn fx:id="policy1ValueColumn" text="Policy 1" prefWidth="180"/>
                                <TableColumn fx:id="policy2ValueColumn" text="Policy 2" prefWidth="180"/>
                                <TableColumn fx:id="policy3ValueColumn" text="Policy 3" prefWidth="180"/>
                            </columns>
                            <placeholder>
                                <Label text="Select policies to compare" styleClass="status-text"/>
                            </placeholder>
                        </TableView>
                    </VBox>
                </HBox>

                <!-- Recommendation -->
                <HBox alignment="CENTER">
                    <VBox maxWidth="1100" styleClass="profile-edit-card" alignment="CENTER">
                        <Label text="Recommendation" styleClass="section-title" alignment="CENTER" maxWidth="Infinity"/>

                        <Label text="Best Value Policy:" style="-fx-font-weight: bold; -fx-font-size: 14px;" alignment="CENTER" maxWidth="Infinity"/>
                        <Label fx:id="bestValueLabel" style="-fx-text-fill: #27ae60; -fx-font-size: 16px; -fx-font-weight: bold;" alignment="CENTER" maxWidth="Infinity"/>

                        <Label text="Recommendation Reason:" style="-fx-font-weight: bold; -fx-font-size: 14px;" alignment="CENTER" maxWidth="Infinity"/>
                        <TextArea fx:id="recommendationArea" prefHeight="80" editable="false"/>
                    </VBox>
                </HBox>

                <Label fx:id="statusLabel" styleClass="status-text" alignment="CENTER" maxWidth="Infinity"/>

                <HBox alignment="CENTER">
                    <Button fx:id="backButton" text="Back to Dashboard" styleClass="button-secondary" prefWidth="150"/>
                </HBox>

            </VBox>
        </ScrollPane>
    </center>

    <bottom>
        <fx:include source="FooterComponent.fxml"/>
    </bottom>
</BorderPane>